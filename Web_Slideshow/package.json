{
  "name": "wedding-web-slideshow",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based wedding photo slideshow for a shared photo directory.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "sharp": "^0.35.4"
  }
}